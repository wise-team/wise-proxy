include /conf/root.conf;

http {
    include /conf/http.conf;

    server {
        include /conf/server_listen.conf;

        #§ 'server_name ' + data.config.environments.staging.host + ';'
        server_name dev.wise.jblew.pl;

        #§ 'ssl_certificate /cert/' + data.config.environments.staging.host + '/fullchain.pem;'
        ssl_certificate /cert/dev.wise.jblew.pl/fullchain.pem;
        #§ 'ssl_certificate_key /cert/live/' + data.config.environments.staging.host + '/fullchain.pem;'
        ssl_certificate_key /cert/live/dev.wise.jblew.pl/fullchain.pem;
        #§ 'ssl_trusted_certificate /cert/live/' + data.config.environments.staging.host + '/chain.pem;'
        ssl_trusted_certificate /cert/live/dev.wise.jblew.pl/chain.pem;

        include /sites/default.conf;
    }
}
